# v2026.07.29-01-rc.2

- 작성일: 2026-07-29
- 상태: 사전 릴리스
- 기준 버전: v2026.07.29-01-rc.1
- 작업 브랜치: `feature/2026-07-29-supply-price-engine`
- 계산 규칙: `supply-model-v2`

## 변경 내용

- 기존 5페이지 동시 호출을 제거하고 요청당 1페이지씩 순차 수집
- 첫 페이지에서 전체 행·페이지 수를 먼저 확인
- 같은 페이지를 최대 4회까지 지수 간격으로 자동 재시도
- 성공한 페이지마다 수집 상태를 D1에 즉시 중간 저장
- 실패 후 재요청하면 완료한 페이지를 건너뛰고 실패 페이지부터 재개
- 실패 페이지, 시도 횟수, HTTP 상태, 건축HUB 결과 코드와 메시지 표시
- K-apt 전체 세대수와 공급면적 산출에 사용한 세대수 교차검증
- D1 미연결 환경의 엣지 캐시 대체 동작과 저장 모드 진단 제공

## 계산 원칙

공급평당가 산식은 `rc.1`과 같습니다. 전용면적과 주거공용면적을 합산한 공급면적
후보별 평당가를 계산한 뒤 실제 세대수 비율로 가중합니다. K-apt 세대수 차이는
수집 완전성 경고로 표시하며, 공급면적 계산 결과를 임의로 보정하지 않습니다.

## 되돌리기

이 버전은 `main`에 병합하지 않고 Preview에서 시험합니다. 문제가 있으면 Preview
배포를 이전 `rc.1` 커밋으로 되돌리거나 작업 브랜치에서 `rc.1` 태그·커밋을 다시
배포합니다. Production `main`과 공개 버전 `v2026.07.16-01`에는 영향을 주지 않습니다.

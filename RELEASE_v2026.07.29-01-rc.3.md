# v2026.07.29-01-rc.3

- 작성일: 2026-07-29
- 상태: 사전 릴리스
- 기준 버전: v2026.07.29-01-rc.2
- 작업 브랜치: `feature/2026-07-29-supply-price-engine`
- 계산 규칙: `supply-model-v2`
- 수집 규칙: `page-fetch-v2`

## 변경 내용

- 첫 페이지에서 1,000·500·100행을 순서대로 시험하고 가능한 최대 크기 선택
- 선택한 페이지 크기로 요청당 한 페이지만 순차 수집
- 같은 페이지가 세 번 반복 실패하면 1,000→500→100행으로 자동 하향
- HTTP 500 등 일시적 오류를 종료하지 않고 D1에 `paused` 상태로 저장
- 5초·15초·30초·1분·2분·5분 간격의 장기 자동 재시도
- F5 새로고침 후 D1의 마지막 성공 지점과 다음 재시도 시각 자동 복구
- 완료한 페이지의 압축 집계 상태만 D1에 중간 저장
- 수집 완료 후 진행 상태를 제거하고 최종 공급면적 프로필만 영구 보관
- 기존 `rc.2`에서 완성된 `supply-model-v2` 프로필은 재수집 없이 즉시 사용

## 계산 원칙

공급평당가 계산식은 `rc.2`와 같습니다. 건축HUB의 전용면적과 주거공용면적을
합산하고 공급면적 후보별 세대수 비율로 평당가를 가중합니다. 이번 버전은 계산식이
아니라 수집 성능과 장애 복구 방식을 변경합니다.

## 되돌리기

이 버전은 Preview에서 검증합니다. 문제가 있으면 태그
`v2026.07.29-01-rc.2` 또는 커밋 `d9869a9`를 다시 배포합니다. Production
`main`과 공개 버전 `v2026.07.16-01`은 변경하지 않습니다.

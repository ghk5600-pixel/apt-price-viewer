# Preview deployment marker

This file triggers a fresh Cloudflare Pages Preview deployment for
`v2026.07.29-01-rc.1` after the Preview environment secret is configured.

It does not change application behavior.
